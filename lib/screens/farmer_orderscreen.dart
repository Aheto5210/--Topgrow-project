import 'package:flutter/material.dart';


class FarmerOrderscreen extends StatelessWidget {
  const FarmerOrderscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(child: Text(' Farmer Order Screen '))
    );
  }
}
